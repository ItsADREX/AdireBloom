PAYSTACK_SECRET_KEY=sk_test_REPLACE_WITH_YOUR_SECRET_KEY
# Live production (Render only): sk_live_... — see PAYSTACK-LIVE.md
JWT_SECRET=change_this_to_a_long_random_string_in_production
# Supabase → Settings → API → JWT Secret (verifies Supabase login tokens on payment routes)
SUPABASE_JWT_SECRET=your-supabase-jwt-secret
PORT=4000
FRONTEND_URL=http://localhost:5173,https://adirebloomtest.netlify.app
